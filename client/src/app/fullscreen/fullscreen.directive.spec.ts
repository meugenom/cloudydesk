import { Fullscreen } from './fullscreen.directive';

describe('FullscreenDirective', () => {
  it('should create an instance', () => {
    const directive = new Fullscreen();
    expect(directive).toBeTruthy();
  });
});
