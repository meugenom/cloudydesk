import { Dir } from './dir.model';

/**
 * Interface for the DirState model
 * @interface DirState
 * @param {Dir} root - The root of the directory tree  
 */
export interface DirState {
	dirs : Dir;
  }